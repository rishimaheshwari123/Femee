// Main Theme Export
import colors from './colors';
import spacing, { containerSizes, borderRadius } from './spacing';
import typography from './typography';

export const theme = {
  colors,
  spacing,
  containerSizes,
  borderRadius,
  typography,
};

export { colors, spacing, containerSizes, borderRadius, typography };
export default theme;
